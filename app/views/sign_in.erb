<div class="login-popup sign-in">
  <% if @error %>
    <div class="errors"><%= @error %></div>
  <% end %>

  <form id="sign-in-form" method="post" action="/sessions">
    <div class="form-title">Sign In</div><br>
    <label class="label" for="email">Email</label>
    <input type="email" name="email" placeholder="e.g., me@example.com" value="<%= @email %>"><br>
    <label class="label" for="password">Password</label>
    <input type="password" name="password" placeholder="6 or more characters">
   <div class="buttons">
    <a class="submit">Submit</a>
    <a class="close">Close</a>
  </div>
  </form>

</div>
